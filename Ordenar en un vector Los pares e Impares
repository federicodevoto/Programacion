/*
Ordenar pares e impares
Crear un programa y la o las funciones necesarias para que cree un array de 100 números aleatorios del 1 al 1000. 
Una vez creado, mostrar el contenido y después organizarlo de forma que estén juntos los elementos pares y los impares. 
Después, volver a mostrar el array.
*/

/*
¿Qué pasa si lo pruebo con el siguiente vector?
{1,2,4,5,7,8,8,6,3,5}
*/

#include<stdio.h>
#include<stdlib.h>
#define TAM 20

void cargarVector(int *);
void mostrarVector(int*);
void ordenarVector(int*); // Agrupa pares e impares

void main()
{
    int vec[TAM];
    cargarVector(vec);
    printf("\n VECTOR: ");
    mostrarVector(vec);
    ordenarVector(vec);
    printf("\n VECTOR MODIFICADO: ");
    mostrarVector(vec);
}

void cargarVector(int *v)
{
    int i;
    srand(time(NULL)); // Para tener distintos numeros en cada ejecucion.
    for(i=0;i<TAM;i++)
    {
        *v=(1+rand()%1000);
        v++;
    }
}

void mostrarVector(int *v)
{
    int i;
    for(i=0;i<TAM;i++)
    {
        printf("%d ",*v);
        v++;
    }
}

void ordenarVector(int *v)//ordena: PARES IMPARES
{
    int *impar,i,aux; //Utilizo 2 puntos, v=par y impar;
    impar=v+TAM-1;//mando al final

    while(v!=impar && v<impar)
    {
        if(*v%2==0) // es par
            v++;
        else
        {
            if(*impar%2!=0) // es impar
                impar--;
            else // invierto y sigo
            {
                aux=*v;
                *v=*impar;
                *impar=aux;
                impar--;
                v++;
            }
        }
    }

}
